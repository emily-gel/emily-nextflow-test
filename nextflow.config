docker.enabled = true

process {
    withName: COWSAY {
        container   = "docker/whalesay"
        }
}

process {
    cpus = 2
    memory = 1 GB
    executor = 'awsbatch'
    container = "emilygel/tester"
}

executor.name = 'awsbatch'
